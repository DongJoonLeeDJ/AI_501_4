r = range(1,11)

for i in r:
    print(i)

'''
    eval(    ) -> 혁신적인 함수...
    
    int(  10.22   ) -> int 로 바꿔주는함수
    float(  )
    bool(  0  ) -> True,False
    str( 1.2 ) -> 문자열로.. 바꿔주는.
    list(    ) -> list..로바꿔
    tuple(    ) -> 
'''