r = range(1,11)

for i in r:
    print(i)

'''
    eval( '1+2+3' ) -> 혁신적인 함수...
    
    int(  10.22   ) -> int 로 바꿔주는함수
    float( '222.333' )
    bool(  ''  ) -> True,False
    str( '1091010' ) -> 문자열로.. 바꿔주는.
    list( range(10,1,-1)   ) -> list..로바꿔
    tuple(    ) -> 
    
    객체 밖에 있는 함수
    len([1,2,3,4,5]), max('abcdef'), min([1,2,3,4,5])
    객체 안에 있는 함수
    count, remove, pop, append, extend
    
    슬라이싱 문법
    a[0:4]
    a[:4]
    a[:]
    a[0:-1]
    
    제어문 
    if elif else for while break contine switch 
'''

alist = [1,3,4,2,10,99,100,2,3,4]
cnt = alist.count()
alist.sort()






